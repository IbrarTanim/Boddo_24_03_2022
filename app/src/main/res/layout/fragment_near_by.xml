<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nearByLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <EditText
        android:id="@+id/other_user_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:hint="Enter User Id"
        android:visibility="gone" />

    <Button
        android:id="@+id/find_user"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/other_user_id"
        android:layout_centerInParent="true"
        android:text="Search User"
        android:visibility="gone" />

    <Button
        android:id="@+id/hotlist"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/find_user"
        android:layout_centerInParent="true"
        android:text="Hotlist"
        android:visibility="gone" />

    <Button
        android:id="@+id/allUser"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/hotlist"
        android:layout_centerInParent="true"
        android:text="All user Implemented"
        android:visibility="gone" />

    <Button
        android:id="@+id/photoblog_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/other_user_id"
        android:layout_centerInParent="true"
        android:layout_centerHorizontal="true"
        android:text="Photo Blog" />
</RelativeLayout>
